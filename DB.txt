SET SERVEROUTPUT ON;

DECLARE -- ONDE DECLARA AS VARIAVEIS
    V_DOLARES NUMBER := 45;
    V_REAIS NUMBER;
    V_CAMBIO NUMBER := 5.50;
    V_VALOR NUMBER;
BEGIN --ONDE FAZ O PROCESSAMENTO
    V_REAIS := V_DOLARES * V_CAMBIO;
    DBMS_OUTPUT.PUT_LINE('REAIS: R$'|| to_char((V_REAIS), '9990.99'));
END;